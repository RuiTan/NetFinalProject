#pragma once

using namespace System;

namespace CalculateLike {
	public ref class Class1
	{
		// TODO: 在此处为此类添加方法。
		static int updateMovieRate(int rate, int likes, int score);
		static int updateMovieRate(int rate, int likes, int rateScore, int score);
	};
}
